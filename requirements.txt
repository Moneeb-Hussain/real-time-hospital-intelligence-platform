# Vercel FastAPI service — keep flat (no nested -r includes).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.0,<3.0
python-dotenv>=1.0,<2.0
supabase>=2.0,<3.0
httpx>=0.27,<1.0
postgrest>=0.16,<1.0
